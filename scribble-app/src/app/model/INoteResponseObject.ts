export interface INoteResponseObject {
    author: string;
    createdAt: string;
    description: string;
    id: number;
    title: string;
    updatedAt: string;
    dueDate: string;
    label: string;
}